@model List<TireSearchMVC.Models.BrandModel>
@{
    ViewData["Title"] = "Brand Search";
    Layout = "_Layout";
}

<div class="header">
    <h1 class="title">Brand Search</h1>
    <p class="subtitle">Araç marka listesini görüntüleyin ve arayın.</p>
</div>

<div class="search-container">
    <form asp-action="Index" asp-controller="Brand" method="get" class="search-form">
        <div class="search-box">
            <input type="text" name="search" value="@ViewBag.Search" placeholder="Marka ara..." class="search-input" />
            <button type="submit" class="search-btn">Ara</button>
        </div>
        <div class="search-stats">
            <span>Toplam: @Model.Count</span>
        </div>
    </form>
</div>

@if (Model.Count == 0)
{
    <div class="empty-state">
        <h3>Kayıt bulunamadı</h3>
    </div>
}
else
{
    <div class="results-grid">
        @foreach (var brand in Model)
        {
            <div class="tire-card">
                <h3>@brand.Description</h3>
                <div class="tire-info">
                    <span><strong>ID:</strong> @brand.UbyId</span>
                    @if (brand.ModifiedTime != null)
                    {
                        <span><strong>Güncellendi:</strong> @brand.ModifiedTime.Value.ToShortDateString()</span>
                    }
                </div>
            </div>
        }
    </div>
}
