@model List<TireSearchMVC.Models.BrandCodeDescriptionModel>
@{
    ViewData["Title"] = "Brand Code Descriptions";
    Layout = "_Layout";
}

<div class="header">
    <h1 class="title">Brand Code Descriptions</h1>
    <p class="subtitle">Marka kodlarını ve açıklamalarını görüntüleyin.</p>
</div>

<div class="search-container">
    <form asp-action="Index" asp-controller="BrandCodeDescription" method="get" class="search-form">
        <div class="search-box">
            <input type="text" name="search" value="@ViewBag.Search" placeholder="Ara..." class="search-input" />
            <button type="submit" class="search-btn">Ara</button>
        </div>
        <div class="search-stats">
            <span>Toplam: @Model.Count</span>
        </div>
    </form>
</div>

@if (Model.Count == 0)
{
    <div class="empty-state">
        <h3>Kayıt bulunamadı</h3>
    </div>
}
else
{
    <div class="results-grid">
        @foreach (var b in Model)
        {
            <div class="tire-card">
                <h3>@b.Description</h3>
                <div class="tire-info">
                    <span><strong>Code:</strong> @b.Code</span>
                </div>
            </div>
        }
    </div>
}
